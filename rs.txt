# Boying 奖励塑形记录 (Reward Shaping Log)
# 路径: legged_gym/legged_gym/envs/boying/boying_config.py
# 对照基线: GO1 (Jul21_01-34-58_, 2999 iters)
# 更新日期: 2026-07-24
================================================================================

## GO1 基线参数（对照参考）
  orientation        = -0.2
  ang_vel_xy         = -0.05
  joint_power        = -2e-5
  power_distribution = -10e-6
  action_rate        = -0.01
  smoothness         = -0.01
  dof_acc            = -2.5e-7
  feet_air_time      = 0.1
  [PD] stiffness=28, damping=0.7
  [PPO] schedule=adaptive, learning_rate=1e-3, desired_kl=0.01, entropy_coef=0.01

## 地形课程配置（供参考）
  num_rows = 10，difficulty = row / 10（最高 0.9）
  terrain_proportions = [0.1, 0.1, 0.35, 0.35, 0.1]（70% 台阶地形）
  max_init_terrain_level = 5
  @difficulty=0.33: step_height ≈ 0.109m
  @difficulty=0.38: step_height ≈ 0.118m
  @difficulty=0.40: step_height ≈ 0.122m

## PD 参数实验总结
  k=50, d=2.25（v1~v4, Method A）：raw power 730W，action_rate 比 GO1 差 50%，terrain 卡 3.3
  k=26, d=0.64（v5,   Method B）：raw power 272W，欠阻尼（ζ≈0.35），terrain 退化至 2.19
  k=40, d=1.5 （v6+,  Method C）：raw power 420W，terrain 3.88 峰值，收敛质量最佳 ✓

================================================================================

## 版本历史

---

## v24：关节转动惯量推导的高增益 PD（待训练）

  父版本：可信 v18（Jul28_22-17-43_ / model_25000.pt），从头训练。
  单一概念变量：整组关节 PD 增益。
    stiffness / Kp: 40.0 -> 156.35 N·m/rad
    damping / Kd:    1.5 ->   9.95 N·m·s/rad
  v18 常量：action_scale=0.25、smoothness=-0.01、feet_air_time_threshold=0.3、
    entropy_coef=0.005、adaptive PPO、seed=5；不继承已 reject 的 v23
    smoothness=-0.0125。

  假设：按关节转动惯量推导的增益能够提高位置目标跟踪和接触后的阻尼，
    但高 Kp/Kd 可能导致转矩饱和、冲击、功率/热负担、策略依赖刚性伺服，
    或因所有关节共用同一增益而造成不同关节动态失配。

  启动前门控：从头训练，不 resume；确认无重复 boying 训练进程；检查
    startup NaN/Inf、CUDA/PhysX、转矩裁剪比例和初始 reset 冲击。
  里程碑：200/500/1000/3000/5000/8000/12000/15000/20000/25000/29999。
  对比基线：v18 同 optimizer iteration；若使用 8192 env，还必须按 total
    timesteps 报告，不能与 4096-env v18 只按 iteration 宣称学习更快。
  动力学门控：plane、rough type6/type14；检查 tracking、torque saturation、
    position margin、joint velocity/acceleration、power、impact、slip、reset。
  参数来源补充：ENCOS EC-A6408-P2-25 电机手册给出转子惯量
    62.4 kg·mm²；内部旋转件折算到转子侧合计 63.36737704 kg·mm²，
    减速比 N=25，折算到输出轴 J=39604.61065 kg·mm²
    =0.03960461065 kg·m²。按 Kp=J(2πfn)^2 得 fn≈10 Hz；若忽略
    粘性阻尼 b，Kd=2ζ(2πfn)J-b 对应 ζ≈2。该 J 未包含腿部连杆
    关于各关节且随姿态变化的等效惯量，因此只作为待验证假设。

  短探针：Aug27_19-03-26_v24_kp15635_kd995_smoke，512 env、10轮；
    GPU PhysX/数值启动通过，无 NaN/Inf。由于训练极早期且仅10轮，
    reward/terrain下降不作为保留或否决证据。

  正式 run：Aug27_19-04-20_v24_kp15635_kd995_env8192
  console：/tmp/dreamwaq_v24_kp15635_kd995.log
  command：python -u legged_gym/legged_gym/scripts/train.py --task=boying
    --headless --num_envs=8192 --seed=5 --max_iterations=30000
    --run_name=v24_kp15635_kd995_env8192
  初始 PID：7848。启动门通过：Physics Device=cuda:0、GPU Pipeline enabled，
    已进入训练循环，无 NaN/Inf、CUDA/PhysX traceback。
  监督 heartbeat：dreamwaq-v24-pd，每30分钟，里程碑/异常才通知。
  v24 200门控（持续失败，提前终止）：
    分析文件：analysis/v24_gate_200.json。检查时已到约350轮，terrain
    从约step94起持续为0，并非单点波动。按optimizer iteration对齐：
      mean_reward       -0.725 vs v18@200 5.104
      episode_length      27.73 vs 979.09
      terrain_level        0.00 vs 0.0568
      tracking_lin_vel   0.00414 vs 0.16466
      tracking_ang_vel   0.00318 vs 0.20658
      surrogate        +0.00607 vs -0.00088
      value_loss         0.01744 vs 0.00416
      autoenc_loss         9.981 vs 5.077
    按total timesteps对齐，v24@200应比较v18@400：v18 reward=7.985、
    episode=976.12、terrain=0.0691、linear tracking=0.2808，结论更差。
    虽然dof_acc/action_rate/smoothness数值较小，但这是机器人快速失败、
    几乎不产生有效运动的退化结果；joint_power较v18@200恶化54%，
    power_distribution恶化约7.64倍。GPU/PhysX无异常。
    判定：reject。Kp=156.35/Kd=9.95统一用于全部关节时，策略无法形成
    有效早期运动和课程学习。约iteration362按持续崩溃停止，不恢复。

## v25：保持惯量公式/阻尼比，闭环频率10 Hz降至5 Hz（待训练）

  父版本：v18配置；v24只作为失败诊断，不继承checkpoint。
  单一概念变量：惯量公式中的目标自然频率 fn 10 Hz -> 5 Hz，保持
    J=0.03960461065 kg·m²与忽略b时ζ≈2不变。因此：
      Kp = 156.35 / 4 = 39.0875 N·m/rad
      Kd = 9.95 / 2 = 4.975 N·m·s/rad
  其他配置保持v18：action_scale=0.25、smoothness=-0.01、seed=5。
  可证伪预测：相对v24，早期episode/reward/tracking不再持续崩溃，
    terrain能够离开0；相对v18，较高Kd可能降低速度/冲击，但也可能
    造成响应迟滞和跟踪恶化。
  短探针：Aug27_19-38-48_v25_fn5_zeta2_smoke，512 env、50轮。
    GPU PhysX与数值健康；step49 episode_length=356.45、linear tracking
    =0.1300、angular tracking=0.0848，明显优于v24早期近零跟踪，说明
    5 Hz方向恢复了部分可学习性。terrain仍降至0.5066且reward=-17.91，
    早期尚不能判keep；进入8192-env正式run，在200门控与v18同步判断。
  正式run：Aug27_19-42-58_v25_fn5_zeta2_env8192
  console：/tmp/dreamwaq_v25_fn5_zeta2.log
  command：python -u legged_gym/legged_gym/scripts/train.py --task=boying
    --headless --num_envs=8192 --seed=5 --max_iterations=30000
    --run_name=v25_fn5_zeta2_env8192
  初始PID：14917。GPU PhysX启动正常，无NaN/Inf；从头训练。
  v25 200门控：analysis/v25_gate_200.json。
    按optimizer iteration，v25 vs v18@200：reward 4.243 vs 5.104
    （-16.9%），episode 960.85 vs 979.09（-1.9%），terrain 0.1598
    vs 0.0568；linear tracking 0.2106 vs 0.1647，angular tracking
    0.2103 vs 0.2066。任务学习已恢复，不是v24式崩溃。
    但动力学代价显著恶化：joint_power 约12.3倍、power_distribution
    约121.6倍、dof_acc约2.36倍，action_rate恶化63.1%，smoothness
    恶化56.5%；lin_vel_z恶化83.7%。按total timesteps对齐v18@400，
    v25 reward低36.3%、linear tracking低10.0%、angular tracking低25.5%，
    功率/分布/加速度差距仍很大。noise=0.631，surrogate仍轻微负，
    无NaN/GPU异常。
    判定：继续到500确认，不因首个里程碑单点停止；500重点检查高Kd
    是否持续造成极端功率分布和加速度，若无恢复则判定进一步计算无价值。
  状态：正式训练中，等待500轮门控。

---

### v1 — 初始 Boying 配置（Jul21_14-32-27_, 15400 iters）
变更（相对 GO1）：
  orientation        -0.2  → -0.3      (高重心补偿)
  joint_power        -2e-5 → -1e-5     (减半：19kg 自然功耗更高)
  power_distribution -10e-6 → -2e-6    (减小：结构不对称)
  action_rate        -0.01 → -0.015    (增大：k=50 高刚度振荡)
  smoothness         -0.01 → -0.008    (调整)
  dof_acc            -2.5e-7 → -1.5e-7 (减小：大腿 1.5kg vs GO1 0.9kg)
  feet_air_time      0.1  → 0.15       (增大：腿长 0.49m vs 0.43m)
  [PD] k=50, d=2.25

结果：
  terrain_level 卡死在 3.2~3.4，全程无进展（GO1 同期达到 3.98+）
  mean_reward 最终约 14.5（GO1 约 18.4）
  feet_air_time 全程为负（-0.009~-0.010），步态无改善
  episode_length 高（920~980）但 terrain 不进：robot 学会"在低难度地形活很久"
  结论：action_rate 和 smoothness 过于严格，抑制了动态步态

---

### v2 — 放宽振荡惩罚（Jul22_11-14-13_, 4388 iters）
变更：
  action_rate        -0.015 → -0.01    (与 GO1 对齐)
  smoothness         -0.008 → -0.01    (与 GO1 对齐)
  power_distribution -2e-6  → -5e-7    (进一步放宽)
  [PD] k=50, d=2.25（不变）

结果：
  terrain_level 最终 3.33（v1 同期 3.25，略好）
  早期收敛更快：1500 步达到 3.22（v1 需 2500 步）
  terrain_level 仍卡在 3.2~3.3，未能突破
  GO1 对比分析发现：
    - action_rate 原始值比 GO1 高 50%（k=50 的结构性振荡）
    - smoothness 原始值比 GO1 高 56%
    - 原始 joint_power 约 693W（GO1 约 92W，真机实测 200~300W）
    - noise_std 比 GO1 高 41%，策略收敛性更差
  结论：惩罚 scale 不是根本问题，k=50 高刚度才是振荡来源

---

### v3 — 错误尝试：加重 joint_power 惩罚（Jul22_13-10-22_, 2479 iters）
变更：
  joint_power        -1e-5 → -2e-5     (恢复 GO1 值)
  [PD] k=50, d=2.25（不变）

结果（与 v2 同步数对比）：
  terrain_level @3000: 3.15（v2 为 3.25，变差 -3.1%）
  mean_reward @3000:   11.12（v2 为 13.21，变差 -15.8%）
  原始 joint_power: 661W（v2 为 693W，仅降 5%）
  结论：scale 加倍但实际功耗几乎不变，robot 物理行为未改变，
        只是相同功耗被扣更多分，奖励景观变差，训练更困难。
        惩罚压不住物理约束，方向错误。

---

### v4 — 放宽 joint_power 惩罚（Jul22_14-24-42_, 2846 iters）
变更：
  joint_power        -2e-5 → -5e-6     (进一步放宽，约 GO1 的 1/4)
  [PD] k=50, d=2.25（不变）

理由：
  Boying sim 功耗约 693W，真机电气功率 200~300W。
  sim 功率虚高约 3x 来自 k=50 高刚度振荡，惩罚无法改变物理特性。

结果（与 v2 对比，2846 iters）：
  terrain_level @2846: 3.36（v2 同期 3.25，+3.3%）
  原始 joint_power:    728W（v2 为 737W，几乎不变！）
  noise_std @2846: 0.555（v2 为 0.531，更高）→ 策略收敛性反而更差
  结论：joint_power scale 调整对 terrain 有微弱正效果（+3%），
        但根本问题仍是 k=50 导致的振荡和高功耗，继续在此方向调参已到边际。

---

### v5 — 切换 PD 至 Method B（Jul22_15-43-58_, 6936 iters）
变更：
  stiffness  50.  → 26.   (Method B：基于二阶系统等效惯量推导)
  damping    2.25 → 0.64

理由：
  v1~v4 共 4 次训练，joint_power scale 从 -2e-5 调到 -5e-6，
  raw 功耗始终约 730W，reward tuning 对物理特性无效。
  k=50 是振荡根源，切换至 Method B 期望降低功耗并改善振荡。

结果：
  raw joint_power 大幅下降：v2(730W) → v5(272W)（降 63%，接近真机 200-300W）
  terrain_level 出现退化性崩溃：step 3200 达到峰值 3.22，step 6936 时仅 2.19
  action_rate 比 v2 差 36%，smoothness 差 38%——低刚度+低阻尼反而更抖
  noise_std 全程 0.68~0.75，策略无法稳定

  根本原因：ζ = 0.64/(2√(26×0.032)) ≈ 0.35，欠阻尼系统会振荡。
  Boying 大腿更重（1.5kg vs GO1 0.9kg），低 k 下欠阻尼振荡更剧烈。
  Method B 理论值在 Boying 上失效，不能直接套用。

---

### v6 — Method C: k=40, d=1.5（Jul22_19-00-58_, 16219 iters）✓ 当前最佳
变更：
  stiffness  26. → 40.   (Method C：k=26 和 k=50 的折中，更高阻尼抑制振荡)
  damping    0.64 → 1.5

结果：
  [重大突破] terrain_level 首次持续上升，无退化：
    step 3200: 3.45，step 8200: 3.75，step 16219: 3.75（峰值 3.88 @step ~12000）
    相比 v2(k=50) 最终 3.40，v6 最终 3.75（+10%）

  raw joint_power 420W（v2 的 57%）；action_rate/smoothness 接近 v2 水平（未恶化）
  mean_reward @16219: 17.93（GO1 为 18.44，差距缩至 3%）
  value_loss @16219: 0.0040（所有 boying 版本最低，接近 GO1 的 0.0035）
  noise_std @16219: 0.504

  深度对比分析（v6 step 12k-16k vs GO1 step 2k-3k）：
    terrain_level 差距仅 -0.5%（3.799 vs 3.880）
    orientation raw 几乎相同（0.037 vs 0.038）—— orientation 不是瓶颈！
    power_distribution raw 24x 差距（4478 vs 185），但 v6 实付惩罚已超 GO1
    action_rate raw 1.51x，smoothness raw 1.49x（k=40 结构性振荡，无法靠 reward 消除）

---

### v7 — orientation 加强至 -0.5（Jul23_00-27-31_, 6236 iters）
变更：
  orientation  -0.3 → -0.5
  [PD] k=40, d=1.5（不变）

结果：灾难性崩溃
  terrain step 3200 达峰值 3.30，step 4600 跌至 0.01，step 5000+ 约 0.0002
  mean_reward @6236: 0.21；noise_std @6236: 0.885；ep_length: 572（频繁摔死）

  根本原因：orientation raw 值与 GO1 相同（0.037 vs 0.038），加大惩罚毫无意义。
  高难度台阶需要轻微前倾，-0.5 的惩罚阻止前倾并引发过激补偿，触发死亡螺旋。
  结论：orientation=-0.3 是 Boying 的安全上限，-0.5 已验证过强，禁止。

---

### v8 — Resume from v6（Jul23_14-45-26_, 18000 iters）
变更：无奖励变更，从 Jul22_19-00-58_ step 16200 恢复，PPO 仍使用 adaptive LR

结果：灾难性退化
  terrain 0-9k 正常（峰值 3.88 @step 8550），之后单调下滑至 2.82（step 18000）
  mean_reward 从 18.8 跌至 0.8（跌幅 96%）；ep_length 从 986 跌至 167
  surrogate loss step 9k 后转正（+0.009），policy 更新方向完全错误

  根本原因：adaptive LR + checkpoint resume 的致命组合
    从已收敛的 checkpoint 恢复时 KL 极低（接近 0），连续触发 "kl < desired_kl/2"
    → lr × 1.5 反复执行，约 40 步内从 5e-4 飙至上限 1e-2
    → 1e-2 大步长破坏已收敛 policy，KL 爆炸
    → lr 被压死到 1e-5，surrogate 转正，无法自愈
    step 8510 第一次脉冲（45 iters），step 8670-8906 第二次脉冲（237 iters，全程 1e-2）

  v6 从头训练时 LR 从未超过 3.84e-4，此次 resume 才触发爆炸。
  与奖励函数和 PD 参数无关，是优化器配置问题。

  修复：在 boying_config.py 中新增：
    schedule = 'fixed'
    learning_rate = 1e-4
  已应用到代码（BoyingRoughCfgPPO.algorithm）。

---

### v9 — Fixed LR resume from v6（Jul24_14-32-17_, 9543 iters）✓ 收敛
变更：
  schedule      adaptive → 'fixed'
  learning_rate 1e-3(adaptive) → 1e-4(fixed)
  从 Jul22_19-00-58_ step 16200 恢复，reward/PD 参数与 v6 完全相同

阶段趋势：
  terrain_level:
    0-2k:   mean=3.54  trend=+1.44（快速重建，checkpoint 起效）
    2k-4k:  mean=3.93  slope=+0.038/1k
    4k-6k:  mean=4.00  slope=+0.052/1k（peak 4.07，首次稳定突破 4.0）
    6k-8k:  mean=3.92  slope=-0.033/1k（小幅回落）
    8k-9.5k:mean=3.95  slope=-0.053/1k（收敛平台）

  noise_std: 从 0.504(v6末尾) 降至 0.445~0.448，fixed LR 效果显著
  surrogate: 全程 -0.0066~-0.0071，稳定学习，无崩溃
  mean_reward: 19.3-19.6，std<0.42，极度稳定（v6 为 17.9）
  terrain≥4.0 占比: 4k-6k 为 24%，8k+ 降至 8.8% → policy 已到当前极限

末尾 raw 值（step 7.5k-9.5k）：
  action_rate raw:  7.43（v6=8.54，改善 13%）
  smoothness raw:   1.04（v6=1.22，改善 15%）
  power_dist raw:   4113（v6=4478，改善 8%）
  orientation raw:  0.033（低于 GO1=0.038，最优）
  tracking_lin_vel: 0.863（GO1=0.85，略高于 GO1）

收敛判断：已收敛
  terrain 在 3.93~4.0 震荡，slope 趋近 0，terrain≥4.0 占比持续下降
  noise_std 锁定 0.446，surrogate 无变化趋势
  4.0 对应 step_height≈0.122m 是当前行为极限，继续训练无法突破

结论：v9 是历史最佳配置，mean_reward 19.6 超越 GO1(18.4)，terrain 到达 4.0 平台。
      当前瓶颈是抬腿高度不足，feet_air_time 阈值过高（0.5s）导致全程无正向激励。

---

---

### v10 — 新 feet_air_time_threshold=0.3 + adaptive LR 从头训练（Jul24_17-54-21_, 12933 iters）✗ 崩溃
变更（相对 v9 / v6 基线）：
  feet_air_time_threshold  0.5 → 0.3   (boying_config.py 覆盖，目标给正向抬腿激励)
  schedule      'fixed' → 'adaptive'   (恢复 adaptive，因为是从头训练)
  learning_rate 1e-4 → 1e-3
  奖励 scale / PD 参数与 v6/v9 完全相同

阶段趋势：
  terrain_level:
    0-1k:   mean=1.69  trend=+0.36（快速上升）
    1k-3k:  mean=3.26  trend=+0.49（持续上升，学习正常）
    3k-7k:  mean=3.58  trend=+0.17（缓慢进展，学习减速）
    7k-8k:  mean=3.74  trend=-0.04（峰值区，3.79 @step ~7600）
    8k-9k:  mean=3.59  trend=-0.05（开始退化）
    9k-12.9k:mean=3.09  slope=-0.25/1k（持续崩溃）
  末尾 terrain: 2.59（比峰值低 1.2 level）

  noise_std: 从 0.60 降至 0.47（7-8k 时），之后逆转回升至 0.61（末尾）
  surrogate: step 7700 后转正（从 -0.005 → +0.005），标志 policy 开始退化
  mean_reward: step 8k 达峰 17.6，之后跌至 2.3（末尾），跌幅 87%
  ep_length: 从 950 跌至 295（末尾），robot 频繁跌倒

末尾 raw 值（最后 2000 步，step 10.9k-12.9k，严重退化期）：
  feet_air_time:      -0.00098  （阈值降低后有改善，v9末尾=-0.0083；但退化后步态已乱）
  tracking_lin_vel:    0.2274   （v9=0.863，严重下降 74%，步速控制崩溃）
  action_rate raw:     3.467    （v9=7.43，看似"改善"，实为 robot 不动了）
  joint_power(W):      272      （v9=394W，同上理由）
  orientation raw:     0.0476   （v9=0.033，变差）

崩溃根本原因（adaptive LR + 新训练的不稳定窗口）：
  LR 范围：min=1e-5，max=1e-2（触达上限！）
  与 v8 的 adaptive LR 爆炸模式相同：
    step 7600 surrogate 接近 0 → KL 降低 → adaptive LR 开始上冲
    step 7700 surrogate 转正（+0.00042）→ policy 更新方向错误
    step 8900 surrogate 跳至 +0.005 → 崩溃加速
  关键差异：v8 是从已收敛 checkpoint resume 时爆炸（step 40 左右），
  v10 是从头训练时在 7600-8000 步自然收敛区爆炸——说明 adaptive LR
  在任何 policy 趋于平稳的阶段都可能触发爆炸，并非只有 resume 时。

  对比 v6（相同参数但 adaptive LR 未爆炸）：
    v6 LR 全程未超过 3.84e-4，v10 在 ~7.6k 步后反复触达 1e-2
    v6 training 过 16k steps 无退化；v10 12.9k 时已崩溃至 2.59
    原因待查：v10 早期进展较快（1-3k 陡升）可能改变了 KL 统计基线

  feet_air_time_threshold=0.3 效果：
    早期 terrain 上升速度与 v6/v9 相近，无明显加速
    末尾 feet_air_time raw 从 v9 的 -0.0083 改善至 -0.001 左右（退化期数据）
    阈值调整对抬腿质量有一定效果，但被崩溃掩盖

结论：
  - feet_air_time_threshold=0.3 是有效的，但崩溃原因与 threshold 无关
  - adaptive LR 在 training 过程中同样会爆炸，不只是 resume 时
  - 后续训练建议统一改为 fixed LR=1e-4（见修复方案），或使用 desired_kl 更保守的值

---

---

### v11 — Fixed LR=1e-4 从头训练（Jul24_23-43-24_, 888 iters）✗ 冷启动失败
变更（相对 v10）：
  schedule      'adaptive' → 'fixed'
  learning_rate 1e-3 → 1e-4

结果：灾难性冷启动失败
  terrain step 0 起单调下降，step 888 时跌至 0.0000（课程底部）
  mean_reward 全程为负（-5 ~ +5，全期基本负值）
  noise_std 全程 0.92~0.95，从未有效下降（v6 同期已降至 0.87 以下）

根本原因：fixed LR=1e-4 与从头训练不兼容（冷启动问题）
  初始策略是完全随机的（noise_std=1.0），奖励为负，地形课程快速下降。
  fixed LR=1e-4 步长极小，每步 policy 更新幅度太小，无法快速逃离负奖励区。
  对比 v6（adaptive LR）早期行为：
    v6 在 step 3-7 内 LR 从 4.4e-4 飙至 5e-3，大步长快速摆脱随机初始化
    v11 全程卡在 1e-4，noise_std @step 40 仍 0.93（v6 同期已至 0.70）
    v6 @step 48 出现第一个正 reward；v11 @step 888 仍未见正 reward

  结论：
  - adaptive LR 在早期（0~300步）是必要的：初始 KL 大 → LR 自动上冲到 1e-3~5e-3
    提供足够大的梯度步让策略快速建立基本行走能力
  - fixed LR=1e-4 只适合 resume 已收敛策略：此时 KL 本已很小，1e-4 足以
  - "adaptive LR 在从头训练时会爆炸"的结论（v10）是有条件的：
    爆炸发生在 step 7600 策略趋于收敛时，而非早期探索阶段
  - 正确方案：warm-up 阶段用 adaptive，收敛时手动或自动切换 fixed

修复方案（v12）：
  先用 adaptive LR=1e-3 训练至 terrain 稳定（约 3000-5000步）
  在 surrogate 接近 0 或 terrain 开始不动时，改为 fixed LR=1e-4 resume
  或：设置更保守的 adaptive LR 上限（max_lr 降至 1e-3，避免 1e-2 峰值）

---

---

### v12 — 降低地形难度 step_height_scale=0.074（Jul25_12-49-59_, 12064 iters）✗ 再次崩溃
变更（相对 v9）：
  step_height_scale  0.18 → 0.074  (最大台阶高度从 0.212m 降至 0.117m)
  schedule           'fixed' → 'adaptive'  (从头训练，恢复 adaptive)
  learning_rate      1e-4 → 1e-3

阶段趋势：
  terrain_level:
    0-1k:   mean=1.76  slope=+3.04/1k（快速爬升）
    1k-4k:  mean=3.79  slope=+0.22/1k（持续上升，突破 v6 峰值 3.88）
    4k-8k:  mean=4.14  slope=+0.01/1k（高位震荡，峰值 4.27 @step ~5700）
    8k-12k: mean=3.41  slope=-0.33/1k（持续崩溃）
  末尾 terrain: 2.95（比峰值低 1.3 level）

  terrain≥4.0 占比（后半段 step 6k+）: 46.1% → 说明 4k-8k 期间大量时间稳定在 4.0 以上
  noise_std: 0.57 → 0.44（step 8k 时最低），之后回升至 0.49
  surrogate: step 8500 从 -0.005 突然转正（+0.003），step 8600 跳至 +0.007 → 崩溃起点
  mean_reward: step 8300 峰值 20.6，崩溃后跌至 14.7（跌幅 29%，比 v10 轻）
  ep_length: 崩溃后仍维持 960+，robot 没有频繁跌倒（与 v10/v8 不同）

LR 爆炸记录：
  step 2600: LR=1e-2（第一次脉冲），surrogate 接近 0 → LR 冲顶，随即回落
  step 5500: LR=2.92e-3（第二次脉冲）
  step 8500: surrogate 转正，LR 被压至 1e-5，此后长期在 1e-5~2.25e-5 徘徊
  → adaptive LR 再次在收敛区爆炸，与 v10 模式完全相同

末尾 raw 值（最后 2000 步，step 10k-12k，退化期）：
  feet_air_time raw:  -0.00657  (v9=-0.0083，有改善，但退化期数据)
  tracking_lin_vel:    0.689   (v9=0.863，下降 20%)
  orientation raw:     0.193   (v9=0.033，严重恶化 → robot 躯干大幅倾斜)
  action_rate raw:     6.949   (v9=7.43，接近)
  smoothness raw:      1.032   (v9=1.04，接近)
  joint_power(W):      352     (v9=394W)

重要发现：
  1. 降低 step_height_scale 有效：terrain 峰值从 v9 的 4.07 提升至 4.27，4k-8k 期间
     terrain≥4.0 占比高达 46%（v9 仅 24%），说明降低地形难度确实让 policy 能到达更高 level
  2. 崩溃原因仍是 adaptive LR 爆炸，与 step_height 无关
     step 8500 surrogate 转正时 LR 已被压至 1e-5，策略更新陷入停滞
  3. orientation raw 末尾 0.193（v9=0.033）：崩溃后 robot 躯干严重倾斜，
     但 ep_length 仍维持 960+，说明 robot 没有摔倒，只是步态质量变差
  4. 本次 adaptive LR 爆炸时间点（step 2600/5500/8500）比 v10（step 7600）更早更频繁，
     可能与新地形难度下 KL 统计特性变化有关

结论：
  - step_height_scale=0.074 方向正确，terrain 峰值和稳定性均优于 v9
  - adaptive LR 爆炸是反复出现的系统性问题，必须根治
  - 下一步：用 fixed LR=1e-4 从头训练（v13），彻底避免 LR 爆炸
  - v11 的冷启动失败是因为 fixed LR=1e-4 初始步长不足，但 v12 已证明 adaptive LR
    在收敛期必然爆炸；需要找到既能冷启动又不爆炸的方案

---

================================================================================

### v13 — adaptive LR + max_learning_rate=5e-4 从头训练（Jul25_17-33-07_, 20266 iters）✗ noise_std 发散
变更（相对 v12 / v9 基线）：
  max_learning_rate 新增   5e-4     (ppo.py 参数化后首次使用，上限从 1e-2 降至 5e-4)
  schedule          不变   adaptive
  learning_rate     不变   1e-3
  step_height_scale 不变   0.074
  rewards / PD      不变

阶段趋势：
  terrain_level：
    0-1k:   2.49→1.23（冷启动震荡）
    1k-4k:  快速爬升至 4.1-4.2
    4k-10k: 稳定震荡在 4.1-4.3（peak 4.49）
    10k-20k: 缓慢上移至 4.2-4.5，posterior(10k+) mean=4.30  max=4.49
    全程 terrain≥4.0 占比: 82.2%；后半段: 100%（从未低于 4.0）

  LR（max_learning_rate=5e-4 生效）：
    min=1e-5, max=5e-4（触达上限 577次=2.8%，触达下限 9次=0.04%）
    实际分布：[1e-5,1e-4)=27.8%，[1e-4,2e-4)=46.7%，[2e-4,3e-4)=18.1%，mean=1.58e-4
    全程未触达 v10/v12 的 1e-2 爆炸区 → LR 爆炸问题已彻底修复

  surrogate：全程负值（-0.004~-0.001），无正值翻转，policy 更新方向正确

  noise_std（新问题）：
    0k: 0.996（随机初始化）
    4k-8k: 0.835~0.841（正常收敛期）
    10k: 0.917（拐点，开始上升！）
    15k: 1.566
    20k: 2.363（远超初始值 1.0，持续发散）

  autoenc_loss：step 0=7623 → step 4k=13.9（正常），之后缓慢回升至 28.3（step 20k）

末尾 raw 值（step 18k-20k）：
  tracking_lin_vel: 0.648（v9=0.863，差距 25%）
  action_rate raw:  13.03（v9=7.43，差距 +75%！）
  smoothness raw:   1.696（v9=1.04，差距 +63%）
  feet_air_time:    -0.0101（v9=-0.0083，步态更差）
  orientation raw:  0.033（v9=0.033，相同——不是瓶颈）
  joint_power:      392W（v9=394W，基本相同）
  mean_reward:      ~10.6（v9=19.6，差距 46%）

LR 爆炸结论：
  ✓ max_learning_rate=5e-4 完全修复 LR 爆炸问题（v10/v12 的反复崩溃已消除）
  ✓ surrogate 全程负值，policy 更新方向始终正确
  ✓ terrain 后半段 100% ≥4.0，稳定性大幅优于 v12（末尾退化至 2.95）

noise_std 发散根本原因：
  PPO loss = surrogate + value_loss - entropy_coef * entropy
  entropy_coef=0.01 → 损失函数主动激励高熵（鼓励探索）
  max_learning_rate=5e-4 过低 → LR 长期压在 1e-4~2e-4（73% 时间 <2e-4）
  低 LR 导致每步 policy 更新幅度极小，无法压制 entropy 项的扩张效应
  → noise_std 从 step 9k 起持续上升（探索压过收敛）
  → 高 noise_std → 动作随机性 ↑ → action_rate/smoothness 恶化 → 奖励下降
  对比 v6：LR 均值 4.65e-4（v13 的 2.9 倍），且可在收敛期自由降至 1e-5，
          entropy 压制充分，noise_std 稳定在 0.50

terrain 停留在 4.0-4.5 分析：
  step_height_scale=0.074 时，level 4 对应 step_height=0.080m，level 9=0.117m
  terrain 在 4.1-4.5 徘徊说明 policy 已能应对 0.083~0.088m 的台阶
  但 noise_std=2.36 导致动作极度抖动，无法稳定执行抬腿动作，阻碍了进一步提升

结论：
  max_learning_rate=5e-4 正确修复了 LR 爆炸，但引入了 noise_std 发散副作用。
  需要同时调整 entropy_coef（降低探索激励）和适当放宽 max_learning_rate。

---

### v14 — entropy_coef=0.001 + resume from v13 step8000（Jul26_01-07-50_, 8076 iters）⚠ LR 死锁
变更（相对 v13）：
  entropy_coef  0.01 → 0.001   (降低 10x，压制 noise_std 发散)
  max_learning_rate 5e-4 → 8e-4 (略微放宽)
  schedule      不变 adaptive
  从 Jul25_17-33-07_ step 8000 恢复（noise_std=0.840，尚未发散的最佳点）

注：日志 step 0 = 实际训练 step 8000，日志 step 8076 = 实际 step ~16076

阶段趋势：
  noise_std（核心指标）：
    日志 step 0: 0.840（resume起点，v13残留值）
    日志 step 1k: 0.425（快速收敛！entropy_coef降低立即生效）
    日志 step 4k: 0.279
    日志 step 8k: 0.249（末尾，持续下降无反弹）
    对比：v13末尾=2.363，v9末尾=0.446，v14末尾=0.249（历史最低）✓

  action_rate raw: 4.18（v9=7.43，改善44%；v13=13.03，改善68%）✓ 历史最佳
  smoothness raw:  0.305（v9=1.04，改善71%）✓ 历史最佳

  tracking_lin_vel: 0.709（v9=0.863，差距18%）— 有提升空间
  mean_reward: 14.7-16.9（v9=19.6，差距约15%）

  terrain_level：
    全程在 4.0-4.3，后期（step>6k）mean=4.07  max=4.16
    terrain≥4.0 占比: 99.8%；terrain≥4.5: 0%
    → terrain 无突破，卡在与 v13 相同的平台

  LR（新问题 - LR 死锁）：
    LR=1e-5（下限）占比: 52.7%；LR>1e-4 仅占 1.4%
    mean=2.05e-5（极低，基本是死的）
    surrogate 全程正值（mean=+0.001，正值占 55.1%）
    → KL 持续 > 2×desired_kl → adaptive LR 反复被压到 1e-5

LR 死锁根本原因：
  从 v13 step 8000（noise_std=0.84）的半发散状态 resume，
  policy 参数处于"高熵但低奖励"的状态，advantage 函数估计误差大。
  entropy_coef 从 0.01 骤降至 0.001 后，policy 快速向低熵收缩，
  每步参数变化幅度超过 clip 范围 → ratio>1 → surrogate 为正 → KL 爆炸
  → adaptive LR 反复被压死在 1e-5 → policy 更新停滞 → terrain 无法进步

结论：
  ✓ entropy_coef=0.001 对 noise_std 的压制效果极强（0.84→0.25，优于 v9）
  ✓ action_rate/smoothness 历史最佳，说明动作平滑性得到根本改善
  ✗ LR 死锁：surrogate 正值 + KL 过大导致 adaptive LR 失效
  ✗ terrain 未能突破 4.0-4.1 平台，policy 学习基本停滞
  ✗ base_height 异常：躯干高度偏差 0.118m（v9/GO1 仅 0.042m，相差 2.8x）
     base_height scale=-1.0（GO1默认）对台阶地形 Boying 过强
     v15 改为 -0.5，给机器人更多空间自然调整躯干高度应对台阶
  根因：从半发散状态 resume 时 entropy 骤降 → KL 过大 → LR 死锁
  解决方案：从头训练（避免 resume 的状态不匹配），或改用 fixed LR

---

================================================================================

## 当前配置（v15，从头训练）
  [PPO] entropy_coef=0.001, max_learning_rate=8e-4
  [rewards] base_height=-0.5
  → 已训练 Jul26_18-59-50_，809 iters，提前终止

### v15 — entropy_coef=0.001 + base_height=-0.5 从头训练（Jul26_18-59-50_, 809 iters）✗ terrain 跌至 0
  terrain 从 step 0 的 2.49 单调下降，step 250+ 跌至 0.001，全程未上升
  noise_std step 200 降至 0.35（过低），step 809 降至 0.25
  mean_reward 稳定在 6-7（policy 学会在 level≈0 平地上存活，但不探索台阶）
  LR step 200+ 长期压在 5e-5，surrogate 接近 0（收敛但收敛到错误策略）

  根本原因：entropy_coef=0.001 使 noise_std 在 step 200 就降至 0.35（GO1 step 4000 才到 0.37）
    policy 过早锁定到"平地确定性策略"，失去探索台阶的随机性
    地形课程因 robot 无法通过台阶持续降级 → terrain 单调跌至 0
    entropy_coef=0.001 与 entropy_coef=0.01 的差异：
      v15 noise_std@step200 = 0.35（过低，失去探索能力）
      v6  noise_std@step200 = 0.65（适中，仍能探索台阶）

  结论：entropy_coef=0.001 过低，回退至 0.01

---

================================================================================

### v16 — entropy_coef=0.01 + max_lr=8e-4 + base_height=-0.5 从头训练（Jul26_19-30-54_, 30000 iters）✗ noise_std 发散
  terrain：0-2k 爬升至 2.9，2k-4k 达 3.45，4k-8k 平台约 3.57-3.65，8k-30k 缓慢爬升至 3.99
  noise_std：step 8000 极小值 0.637（拐点），之后单调发散至 5.233（终点）
    发散速率约 +0.16/1000步，与 v13 相同模式，仅拐点从 9k 提前到 8k
  LR：step 0-8k mean=2.6e-4（正常），step 8k+ 被 KL 压至 7e-5（max_lr=8e-4 此后再未触达）
  autoenc_loss：step 9500 后稳定在 8.1-8.4，完全收敛，未受 noise_std 影响
  mean_reward 末尾（step 25k-30k）：18.39，接近 v9 的 19.6（意外良好）
  tracking_lin_vel 末尾：0.847（接近 v9 的 0.863）
  action_rate 末尾：-0.083（比 v9 更好！noise_std 发散反而带来了多样探索）
  terrain 末尾（step 25k-30k）：mean=3.99，与 v9 相同平台，未能突破

  根本原因：entropy_coef=0.01 在 LR 降至 7e-5 后无法被压制
    PPO loss 中 -entropy_coef×entropy 的梯度 = -entropy_coef/σ，持续推 σ 上升
    adaptive LR 因高 KL 被压低 → 低 LR 无法压制熵激励 → σ 继续上升 → KL 更高 → 螺旋
    max_lr=8e-4 对此无效：实际 LR 在 step 8k 后从未触达 max_lr 上限

  与 v13 对比：
    v13 末尾 noise_std=2.36，mean_reward≈10.6，tracking=0.648（严重恶化）
    v16 末尾 noise_std=5.23，mean_reward=18.39，tracking=0.847（远优于 v13）
    原因：v13 的熵爆炸发生在 terrain 卡住时（纯混乱），v16 的发散伴随 terrain 仍在爬升（有效探索）

  结论：
    - max_learning_rate 调整（5e-4→8e-4）对 noise_std 发散无效
    - entropy_coef=0.01 的根本问题未解决
    - 下一步：entropy_coef=0.005（0.01 和 0.001 的中点）

---

================================================================================

## 当前配置（v17，从头训练）
  [PPO]
  schedule          = 'adaptive'
  learning_rate     = 1e-3
  max_learning_rate = 8e-4
  entropy_coef      = 0.005  # v17：0.01 发散(v13/v16)，0.001 锁死(v15)，0.005 取中点

  [terrain / rewards / PD]  —— 与 v16 完全相同，不变

  训练命令：
    python legged_gym/scripts/train.py --task boying --headless

  关键监控点：
    step  200：noise_std 应在 0.55-0.75（v15=0.35 死，v16=0.73 正常）
    step 8000：noise_std 拐点，看是否仍发散（v16 拐点在此处）
    step12000：与 v16 同期 1.17 对比，若低于 0.8 则方向正确

### v17 — entropy_coef=0.005 从头训练（Jul27_14-35-57_, 30000 iters）✓ 历史最优
  noise_std：step 1k 快速降至 0.443，逐步降至 step 24k 极小值 0.327，末尾轻微回升至 0.455
    全程未发散（v13/v16 发散至 2.36/5.23），末尾回升速率 +0.025/1k（v16 的 1/6）
    FINAL 0.455 ≈ v9 最优 0.446，entropy_coef=0.005 有效压制发散

  terrain：缓慢爬升 → step 15k 稳定在 4.07，FINAL=4.11
    比 v9(4.00)/v16(3.99) 略高，但仍未突破 4.5 平台

  mean_reward FINAL=22.07 ★ 历史最高（v9=19.6，GO1=18.44，超越 GO1 20%）
  tracking_lin_vel FINAL=0.898 ★ 历史最高（v9=0.863，v16=0.847）
  tracking_ang_vel FINAL=0.404 ★ 历史最高
  action_rate FINAL=-0.044 ★ 历史最优（v9≈-0.074，v16=-0.083，改善 40%）
  smoothness FINAL=-0.005 ★ 历史最优
  base_height FINAL=-0.002（近乎零偏差，-0.5 scale 完全生效）

  LR：全程自然衰减（2e-4→6e-5），无死锁（1e-5 仅出现 8次≈0%），无爆炸
  autoenc：step 8k 后收敛至 7.5-8.5，FINAL=7.49（历史最佳）
  surrogate：全程负值，FINAL=-0.0040（仍在加深，未收敛）
  value_loss：FINAL=0.0032（历史最低，仍在下降）

  step 25k+ noise_std 轻微回升原因：terrain 4.1 遇新平台，policy 需更多探索
    此非发散前兆（量级 0.33→0.455，vs v16 的 0.63→5.23）
    末尾回升速率 +0.028/1k（实测），外推至 step 40k ≈ 0.735（接近危险区）

  步态分析（Jul28，从 TensorBoard 日志提取，基于训练数据统计）：
    注：IsaacGym 在 WSL2 中 PhysX GPU pipeline 无法初始化，无法直接 play；
        以下数据来自训练时每 episode 的实际运动统计，1000步/episode，可靠。

    feet_air_time：
      全程 100% 负值（scaled/ep 均值=-0.0100，unscaled/ep=-0.0666）
      threshold=0.5s 整个训练从未被超越，机器人无正向抬腿激励
      步态模式：贴地滑步而非标准抬脚步态（air_time < 500ms 全程）
      对比 v9(threshold=0.5)：末尾 scaled=-0.0083，v17=-0.0100（略差，地形更难）
      结论：threshold=0.5 是地形卡 4.1 的深层原因之一，必须降低

    腿部平滑度（step 5k → 30k 改善幅度）：
      action_rate:  -0.064 → -0.044（改善 31%）★ 历史最优
      smoothness:   -0.072 → -0.047（改善 35%）★ 历史最优
      dof_acc:      -0.046 → -0.042（改善  9%）
      → 腿部协调性持续提升，动作越来越干净

    躯干稳定性：
      ang_vel_xy:   -0.067 → -0.047（改善 31%）
      base_height:  -0.017 → -0.002（改善 88%，近乎零偏差）✓
      → 躯干稳定性正常，高度控制最优

    不跌倒率：
      step 5k: 959/1000 步（96%）
      step 25k: 993/1000 步（99.3%）← 最优
      step 30k: 984/1000 步（98.4%）

    checkpoint 25k vs 30k 对比：
      terrain_level:    25k=4.14 > 30k=4.11  → 25k 胜
      feet_air_time:    25k=-0.00955 > 30k=-0.01003  → 25k 胜
      ang_vel_xy:       25k=-0.046 > 30k=-0.047  → 25k 胜
      base_height:      25k=-0.0017 > 30k=-0.0018  → 25k 胜
      tracking_lin_vel: 25k=0.884 < 30k=0.898  → 30k 胜
      action_rate:      25k=-0.051 < 30k=-0.044  → 30k 胜（更平滑）
      smoothness:       25k=-0.006 < 30k=-0.005  → 30k 胜
      noise_std:        25k=0.328 < 30k=0.455  → 25k 胜（更确定性）
      结论：model_25000.pt 综合步态质量更好（terrain/稳定性/noise_std），
            推荐部署权重；model_30000.pt 动作更平滑但策略稍松散

    当前能跨越台阶高度：≈ 8.0cm
      terrain_level 稳定在 4.1，step_height = 0.05 + 0.074×0.41 = 0.080m

  结论：
    ✓ entropy_coef=0.005 是目前最优配置，noise_std 问题基本解决
    ✓ 奖励质量全面超越历史所有版本
    ✓ 躯干稳定、不跌倒率高、动作平滑度历史最优
    ⚠ 步态为贴地滑步，无正向抬腿激励（feet_air_time 全程负值）
    ⚠ terrain 仍停在 4.1（≈8cm 台阶），feet_air_time_threshold=0.5 是关键瓶颈
    ⚠ noise_std 末尾回升速率 +0.028/1k，继续训练至 50k 有发散风险
    → 不再继续 v17 训练；下一步 v18：feet_air_time_threshold=0.5→0.3 从头训练

---

================================================================================

## 当前配置（v18，从头训练）
  [PPO]
  schedule          = 'adaptive'
  learning_rate     = 1e-3
  max_learning_rate = 8e-4
  entropy_coef      = 0.005  ✓ 当前最优（v17 验证）

  [terrain / PD]  —— 不变

  [变更]
  feet_air_time_threshold = 0.3  # 0.5 → 0.3（v17 步态分析证实 0.5 全程无正向激励）

### v18 — feet_air_time_threshold=0.3 从头训练（Jul28_22-17-43_, 30000 iters）✓ 步态/学习改善，model_25000 为候选
  单变量变更（相对 v17）：
    feet_air_time_threshold 0.5 → 0.3
  其余奖励、PD 和 PPO 参数完全不变。

  最终 TensorBoard 同步对比（step 29999，v18 vs v17）：
    mean_reward:          23.344 vs 22.077（+5.7%）
    terrain_level:         4.125 vs  4.114（+0.3%；末 10% 均值 4.170 vs 4.091）
    tracking_lin_vel:      0.909 vs  0.898（+1.3%）
    tracking_ang_vel:      0.423 vs  0.404（+4.8%）
    mean_episode_length: 994.34  vs 984.44 （+1.0%）
    noise_std:             0.293 vs  0.455（-35.6%，未发散）
    value_loss:            0.00253 vs 0.00324（-22.1%）
    autoenc_loss:          7.391 vs  7.495（-1.4%）
    surrogate:            -0.00846，全程保持健康负值

  最终 episode 奖励项：
    feet_air_time:       -0.00378 vs -0.01003（改善 62.3%）✓ 核心目标达成
    base_height:         -0.00044 vs -0.00181（改善 75.6%）
    orientation:         -0.00503 vs -0.00742（改善 32.2%）
    ang_vel_xy:          -0.03963 vs -0.04661（改善 15.0%）
    tracking_lin_vel:     +1.3%；tracking_ang_vel: +4.8%
    action_rate:          改善 3.3%；lin_vel_z: 改善 4.9%
    dof_acc:              恶化 3.3%
    joint_power:          恶化 9.6%
    power_distribution:   恶化 28.4%
    smoothness:           恶化 12.1%

  解释：
    threshold=0.3 显著减少了负向腾空奖励，并加快早期地形学习；
    但训练迭代均值仍未转正，说明 0.3s 对部分落足周期仍偏长。
    v18 的最终 terrain 与 v17 接近，主要收益是更好的跟踪、姿态和探索稳定性，
    而不是突破 terrain 4.1 平台。训练后期的能耗分布仍弱于 v17。

  确定性动力学评估（gait_analysis.py，固定命令、无噪声/域随机化）：
    平地命令覆盖：vx=0.3、0.6、1.0m/s，以及 vx=0.3m/s+yaw=0.5rad/s；
    楼梯覆盖：curriculum type 6/type 14，中等难度，vx=0.6m/s。

    v17/model_30000：
      平地四场景均扭矩打满（max torque utilization=1.0）
      关节位置越限 508~700 个 sample-joint，最小 margin=-0.077rad
      高速峰值关节速度=1.43x URDF limit
      → TensorBoard 奖励虽高，但不具备直接真机候选资格。

    v18/model_20000：
      姿态最稳，但平地平均绝对机械功率最高（vx=0.6: 373W）
      → 不作为首选。

    v18/model_25000（当前最佳折中）：
      平地 vx=0.6：tracking RMS=0.0266m/s，平均绝对功率=319.8W，
        torque max=0.344x，velocity max=1.007x，位置 margin=+0.353rad，
        stance slip p95=0.156m/s，无 reset/位置越限
      高速 vx=1.0：velocity max=1.004x，位置 margin=+0.221rad，无越限
      楼梯 type6：tracking RMS=0.0639m/s，torque max=0.697x，
        velocity max=1.616x，位置 margin=+0.022rad，无位置越限
      楼梯 type14：velocity max=1.092x，位置 margin=+0.044rad，无位置越限
      → 当前仿真部署候选，但楼梯关节速度仍明显超限。

    v18/model_30000：
      平地平均功率最低（vx=0.6: 277.2W），但横摆误差和高速滑移更大；
      楼梯 type6 velocity max=1.853x，出现 2 个位置越限样本，margin=-0.040rad
      → 不优先于 model_25000。

  真机安全结论（当前）：
    model_25000 只具备“受控真机验证候选”资格，不是可直接自由运行的权重。
    首次验证必须使用吊架、低速命令（≤0.3m/s）、软件速度/位置/扭矩限幅、
    硬件急停和短时分段测试；在平地通过前禁止上楼梯。
    训练仍未覆盖执行器热模型、通信延迟、足端材料差异和真实电池压降。

### v23 — 小幅增强动作目标二阶平滑惩罚（训练中）
  父版本：
    v18（Jul28_22-17-43_，参考 checkpoint=model_25000.pt）。

  已知问题：
    v18 最终 smoothness 相对 v17 恶化 12.1%，dof_acc 恶化 3.3%；
    model_25000 在 rough type6 的 peak velocity utilization=1.616，
    但位置和扭矩仍有余量。v19 直接加入 dof_vel_limits=-0.1 后出现
    扭矩打满、位置越限和滑移，说明直接压关节速度会诱发补偿性高扭矩。

  假设：
    楼梯峰值速度的一部分来自策略动作目标的高频二阶突变，而不是平均步速过高。
    小幅增强 joint position target 的二阶差分惩罚，应降低动作 jerk、dof_acc
    和关节速度峰值，同时比 dof_vel_limits 更少诱发持续扭矩对抗。

  唯一变更（相对 v18）：
    rewards.scales.smoothness: -0.01 → -0.0125（增强 25%）

  保持不变：
    action_scale=0.25；Kp=40、Kd=1.5；feet_air_time_threshold=0.3；
    terrain、初始位姿、其余奖励、PPO、观测和域随机化全部保持 v18。
    不包含已回退的摆动足高度奖励，也不恢复 v19/v20/v21 参数。

  正式运行：
    首段 run: Jul30_23-00-22_v23_smoothness_0125
    console: /tmp/dreamwaq_v23_train.log
    用户要求切换到明确的默认训练环境后，首段于约 iteration 550 安全停止，
    不恢复、不与后续 TensorBoard 曲线拼接。

    第二段 run: Jul30_23-17-32_v23_default_env
    console: /tmp/dreamwaq_v23_default_env.log
    使用默认 4096 env；后按用户要求安全停止，不恢复、不拼接。

    当前 run: Jul30_23-26-13_v23_env8192
    console: /tmp/dreamwaq_v23_env8192.log
    command: python -u legged_gym/legged_gym/scripts/train.py
             --task=boying --headless --num_envs 8192 --run_name v23_env8192
    seed=5，目标 30000 iterations，使用 8192 并行环境从头训练。
    除 num_envs 外未传 terrain 覆盖参数：使用默认
    trimesh curriculum、10 个难度等级行和 20 个地形类型列；
    初始 terrain level 在 0~5 均匀随机，随后按机器人表现独立升降。
    注意：每个 iteration 的环境采样量为 4096-env v18 基线的 2 倍；
    后续必须同时按 iteration 与 total timesteps 同步比较，避免错判收敛速度。
    该段于约 iteration 362 按用户要求安全停止，以便重启并修复 WSLg/Vulkan；
    最后完整 checkpoint 约为 model_350.pt。监督 hook 已暂停，不自动恢复。

    系统修复后当前正式 run: Jul31_00-19-05_v23_env8192_post_repair
    console: /tmp/dreamwaq_v23_env8192_post_repair.log
    command: python -u legged_gym/legged_gym/scripts/train.py
             --task=boying --headless --num_envs 8192
             --run_name v23_env8192_post_repair
    seed=5，目标 30000 iterations，从头训练，不恢复或拼接旧 v23 段。
    启动门控通过：CUDA、GPU PhysX、8192 env 正常，无 NaN/Inf。
    hook dreamwaq-v23 已重新启用并指向本段。

  v23 post-repair 200/500 门控（8192 env，早期继续观察）：
    按 optimizer iteration 同步，step 500 的 v23 vs v18：
      mean_reward:          9.593 vs 9.579（+0.15%）
      episode_length:     875.12  vs 970.95（-9.87%）
      terrain_level:        0.238 vs 0.254（-6.07%）
      tracking_lin_vel:     0.344 vs 0.326（+5.50%）
      tracking_ang_vel:     0.313 vs 0.316（-0.98%）
      dof_acc:             -0.0268 vs -0.0307（改善 12.79%）
      action_rate:         -0.0486 vs -0.0542（改善 10.25%）
      joint_power:         -0.000793 vs -0.000971（改善 18.26%）
      power_distribution:  -0.000453 vs -0.000626（改善 27.56%）
      noise_std:            0.422 vs 0.448（-5.79%，未发散）
      learning_rate:        4.68e-5 vs 1.05e-4
      surrogate:           +0.000552 vs -0.001160

    smoothness 加权项为 -0.007534 vs -0.006928；考虑 v23 权重增强 25%，
    反推未加权二阶差分约改善 13%，方向与假设一致。

    按 total timesteps 对齐（v23 step 500≈v18 step 1000）：
      reward -25.3%、terrain -76.6%、linear tracking -41.0%，
      但 dof_acc/action_rate/joint_power/power_distribution 分别改善
      37.5%/26.7%/40.6%/67.8%。说明 8192 env 在单位样本学习效率上明显更慢，
      同时确实降低了动作动态量。

    门控结论：
      不停止。reward 仍持续上升、无 NaN/PhysX/GPU 异常，且 step 713
      surrogate 已回到轻微负值；但 terrain/episode 与样本效率落后，
      step 450 后 surrogate 约 77% 为小幅正值，是 1000 门控的重点风险。
      若 1000 门控仍持续 surrogate 转正、terrain 不恢复或 LR 继续下锁，
      则应判定大 batch 优化效率不足并考虑停止，而不能只看平滑项改善。
    启动门控已通过：GPU PhysX 正常，无 NaN/Inf 或配置漂移。
    监督 hook: dreamwaq-v23（每 30 分钟，里程碑/异常才通知）。

  v23 post-repair 1000/3000/5000/8000/12000/15000 门控：
    数据文件：analysis/v23_post_repair_gate_15000_iter.json。
    当前检查时训练已到 step 18787，进程持续存活；控制台未发现
    NaN/Inf、CUDA/PhysX、显存或 traceback 异常。

    step 15000，500-step 窗口，按 optimizer iteration 同步（v23 vs v18）：
      mean_reward:          22.858 vs 22.481（+1.68%）
      episode_length:       989.69 vs 988.49（+0.12%）
      terrain_level:         4.209 vs 4.179（+0.71%）
      tracking_lin_vel:      0.8966 vs 0.8912（+0.60%）
      tracking_ang_vel:      0.4163 vs 0.4102（+1.48%）
      dof_acc:              -0.04384 vs -0.04440（改善 1.25%）
      action_rate:          -0.04165 vs -0.04497（改善 7.40%）
      joint_power:          -0.001869 vs -0.001891（改善 1.18%）
      power_distribution:   -0.001693 vs -0.001700（改善 0.39%）
      noise_std:             0.2899 vs 0.3015（-3.86%，未发散）
      surrogate:            -0.00401 vs -0.00707（保持为负）
      value_loss:            0.00318 vs 0.00332（-4.13%）
      autoenc_loss:          7.354 vs 7.396（-0.57%）
      learning_rate:         2.59e-4 vs 1.87e-4（自适应，未死锁）

    按 total timesteps 对齐（v23 step 15000≈v18 step 29999）：
      reward -1.70%、episode length -0.17%、terrain +0.93%；
      linear/angular tracking 分别 -0.88%/-1.42%，均在 5% 门内；
      action_rate 改善 0.64%，但 dof_acc、joint_power、
      power_distribution 分别恶化 3.87%、0.77%、2.58%。
      smoothness 加权项为 -0.006126 vs -0.005173；考虑 v23 惩罚权重
      为 v18 的 1.25 倍，反推未加权二阶差分约为 -0.004901，
      相对 v18 约改善 5.3%，说明目标机制仍成立但收益已明显收窄。

    最新 step 17788-18787 健康窗口：
      reward=23.183、episode_length=990.38、terrain=4.123；
      noise_std=0.2894；surrogate=-0.00429，正值占比仅 0.3%；
      LR 均值 2.80e-4（范围 1e-5~8e-4），无持续正 surrogate、
      noise 发散、LR 死锁或 curriculum/reward 崩溃。

    门控结论：
      继续训练至 20000 门控。v23 已消除 500-step 时的大 batch
      早期学习效率落后，任务/课程指标与 v18 基本等价且动作目标
      二阶平滑原始量改善；但确定性 rollout 尚未验证，不能据此判定
      keep 或真机安全。20000 后应暂停训练，在相同平地与 rough
      type6/type14 场景对 v23/model_20000 和 v18/model_25000 做动力学门控。

  v23 post-repair 20000 TensorBoard + 确定性动力学门控：
    TensorBoard 数据：analysis/v23_post_repair_gate_20000_iter.json。
    Rollout 数据：analysis/gait_v23_20000_gate/（固定 cmd=0.6,0,0，
    warmup=150、record=700、无噪声/域随机化，GPU PhysX 串行运行）。
    评估期间安全暂停训练，完成后已恢复；没有并行 GPU PhysX。

    step 20000，500-step 窗口，按 optimizer iteration 同步（v23 vs v18）：
      mean_reward:          23.496 vs 22.823（+2.95%）
      episode_length:       992.85 vs 989.93（+0.29%）
      terrain_level:         4.203 vs 4.179（+0.57%）
      tracking_lin_vel:      0.9093 vs 0.8967（+1.41%）
      tracking_ang_vel:      0.4272 vs 0.4165（+2.58%）
      dof_acc:              -0.04180 vs -0.04380（改善 4.58%）
      action_rate:          -0.04010 vs -0.04369（改善 8.21%）
      joint_power:          -0.001827 vs -0.001903（改善 4.03%）
      power_distribution:   -0.001580 vs -0.001738（改善 9.05%）
      noise_std:             0.2795 vs 0.2988（-6.45%，未发散）
      surrogate:            -0.00423 vs -0.00696（保持为负）
      value_loss:            0.00288 vs 0.00316（-8.92%）
      autoenc_loss:          7.421 vs 7.397（+0.32%）
      learning_rate:         2.16e-4 vs 1.68e-4（未死锁）
    smoothness 加权项 -0.005933 vs -0.005379；扣除 1.25 倍权重后，
    v23 未加权二阶差分约 -0.004746，相对同步 v18 改善约 11.8%。
    v23 step20000 对应的样本量需要 v18 step40000，但基线仅到 29999，
    因而不存在严格的 total-timesteps 同步点；不能伪造样本对齐结论。

    确定性 rollout（v23/model_20000 vs v18/model_25000）：
      plane：
        tracking RMS 0.03412 vs 0.02659（v23 恶化 28.3%，超过 10% 门）；
        velocity max 1.009 vs 1.007，torque max 0.362 vs 0.344；
        position margin 0.319 vs 0.353，均零越限/零 reset；
        action-rate RMS 10.85 vs 11.65、dof-acc RMS 91.72 vs 97.21、
        mean total power 294.6 vs 319.8，动态量/功率改善；
        但 vertical-velocity RMS 0.0316 vs 0.0211、stance-slip RMS
        0.1725 vs 0.1318，平地垂向运动和滑移明显恶化。
      rough type6 level5：
        tracking RMS 0.06227 vs 0.07879（改善 21.0%）；
        velocity max 1.279 vs 1.006（v23 未通过 <1.10 门）；
        position margin -0.0681、8 次越限 vs -0.0375、2 次越限；
        torque max 0.467 vs 0.493，均零 reset。
      rough type14 level5：
        tracking RMS 0.05393 vs 0.10097（改善 46.6%）；
        velocity max 1.249 vs 1.509（明显改善但仍未通过 <1.10 门）；
        position margin -0.0334、3 次越限 vs -0.0996、22 次越限；
        torque max 0.456 vs 0.753，均零 reset。

    门控分类：inconclusive / 暂不 keep。
      TensorBoard 和 rough type14 支持更强 smoothness 可降低部分动态量，
      但主要假设尚未稳定成立：type6 速度峰值反而升高，且位置越限增多；
      平地 tracking、垂向速度和滑移也违反既定质量门。由于这只是首个
      中后期 checkpoint，按协议不因单次 rollout 停止；训练继续到
      model_25000，随后在同一三场景复测。若 25000 仍重复 type6
      velocity/position 与 plane tracking/slip 失败，则 v23 应 reject，
      不应继续消耗到 30000 或进入真机候选。

  v23 post-repair 25000 最终门控（提前终止）：
    TensorBoard 数据：analysis/v23_post_repair_gate_25000_iter.json。
    Rollout 数据：analysis/gait_v23_25000_gate/（固定 cmd=0.6,0,0，
    warmup=150、record=700、无噪声/域随机化，GPU PhysX 串行运行）。
    评估期间暂停训练；门控失败后按 20000 时预注册的停止条件终止，
    最后完整 checkpoint 为 model_25000.pt，未继续到 30000。

    step 25000，按 optimizer iteration 同步（v23 vs v18）：
      mean_reward +4.42%、episode_length +2.43%、terrain +0.38%；
      tracking_lin_vel +2.72%、tracking_ang_vel +3.61%；
      dof_acc、action_rate、joint_power、power_distribution 分别改善
      4.27%、6.10%、2.55%、6.07%；noise_std=0.2846（未发散），
      surrogate=-0.00536（保持为负），value_loss 改善 8.94%，
      autoenc_loss 基本相当，LR=2.56e-4（未死锁）。
      v23 step25000 对应 v18 step50000，但 v18 只到 29999，
      因而没有严格 total-timesteps 同步点；以上只能解释为 iteration
      同步结果，不能将 2 倍采样量忽略。

    确定性 rollout（v23/model_25000 vs v18/model_25000）：
      plane：
        tracking RMS 0.03817 vs 0.02659（v23 恶化 43.6%，未通过≤10%门）；
        velocity max 1.008、torque max 0.302、position margin 0.350，
        零越限/零 reset；但 stance-slip RMS 0.2046 vs 0.1318（恶化55.2%）。
      rough type6 level5：
        tracking RMS 0.07608 vs 0.06001（恶化26.8%）；
        velocity max 1.821 vs 1.440（两者均失败，v23 更差且远高于1.10）；
        position margin 0.0100，虽零越限但只剩很小余量；
        torque max 0.577，零 reset。
      rough type14 level5：
        tracking RMS 0.07019 vs 0.07031（基本相当）；
        velocity max 1.320 vs 1.650（v23 改善但仍未通过1.10）；
        position margin 0.0805、零越限，torque max 0.437、零 reset。

    最终判定：reject v23，不是真机候选。
      加强 smoothness 确实改善训练标量中的动作、加速度和功率惩罚，
      但没有把奖励代理收益稳定转化为部署相关的动力学质量；25000
      再次出现平地 tracking/slip 失败和 type6 速度峰值失败，且相对
      20000 的 type6 velocity max 1.279 进一步恶化至 1.821。
      因此该单变量假设被确定性 rollout 证伪，训练在约 25780 iteration
      有意停止，不自动启动下一版本。保留 v18/model_25000 作为可信基线。

  可证伪预测：
    同步 step 下 smoothness 和 dof_acc 原始量应改善，且中后期 checkpoint
    的 rough type6 peak velocity utilization 应低于 v18/model_25000；
    若仅提高奖励而未降低确定性 rollout 的速度峰值，则机制不成立。

  门控：
    200/500/1000：数值稳定、reward/episode/terrain 不持续崩溃；
    3000/5000/8000/12000/15000/20000/25000/29999：
      与原始 v18 做相同步数、相同窗口的全部 TensorBoard 对比。
    中后期使用相同平地命令和 rough type6/type14 评估多个 checkpoint。

  最终安全门：
    rough type6 peak velocity utilization<1.10；
    position margin>0、零位置越限、torque utilization<1.0；
    平地 tracking RMS 相对 v18/model_25000 恶化≤10%；
    同步 terrain/reward 不持续低于 v18 超过 5%；
    同时检查功率、滑移、碰撞、reset 和动作/加速度峰值。
    通过仿真门控也不等同真机安全，仍要求吊架、低速、软硬限幅和急停。

---

## 已废弃方向
  - orientation 加强（>-0.3）：v7 证明致命
  - power_distribution 加大：v6 实付惩罚已超 GO1
  - joint_power 加大：raw 功耗不受 scale 影响
  - adaptive LR + 收敛 checkpoint resume：v8 证明致命
  - fixed LR=1e-4 从头训练（冷启动阶段）：v11 证明失败
  - adaptive LR 无上限（max_lr=1e-2）：v10/v12 均在收敛期爆炸
  - max_learning_rate=5e-4 + entropy_coef=0.01：v13 证明 noise_std 发散
  - entropy_coef 骤降（0.01→0.001）+ resume 半发散 checkpoint：v14 证明 LR 死锁
  - entropy_coef=0.001 从头训练：v15 证明 policy 过早收敛到平地策略，terrain 跌至 0
  - max_learning_rate 调整（5e-4/8e-4）：v13/v16 证明无法阻止 noise_std 发散（LR 被 KL 压低，非 max_lr 限制）
  - base_height=-1.0（GO1默认）：v14 证明 Boying 台阶地形下躯干偏差 2.8x 过大，改为 -0.5

================================================================================
