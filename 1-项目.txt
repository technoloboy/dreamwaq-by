conda activate dreamwaq


python3 legged_gym/legged_gym/scripts/train.py --task=go1 --headless


pos = [0.0, 0.0, 0.40] # x,y,z [m]
default_joint_angles = { # = target angles [rad] when action = 0.0
           ".*R_hip_joint": -0.1,                                                                                        
           ".*L_hip_joint": 0.1,                                                                                         
           "F[L,R]_thigh_joint": 0.8,                                                                                    
           "R[L,R]_thigh_joint": 1.0,                                                                                    
           ".*_calf_joint": -1.5, 
}

control_type = 'P'
stiffness = {'joint': 60.}  # [N*m/rad]
damping = {'joint': 4.5}     # [N*m*s/rad]
# action scale: target angle = actionScale * action + defaultAngle
action_scale = 0.25
# decimation: Number of control action updates @ sim DT per policy DT
decimation = 4

/plan 根据代码完整分析'/home/wkl/DreamWaQ'整个项目，其中训练任务python3 legged_gym/legged_gym/scripts/train.py --task=go1 --headless整个的训练过程是如何的？尤其关注与go1机器人本体配置相关的文件，比如，机器人的模型，关节相关的限位和控制参数，以及加载环境中机器人关节位置和高度的配置参数。我让你了解这些是因为，我想创建一个新的任务，相较于go1任务只有机器人本体变化（也有和机器人控制相关的参数变化），新的机器人为'/home/wkl/DreamWaQ/legged_gym/resources/robots/boying_description',然后新的机器人一些重要配置如下。关于新任务创建，要尽可能创建新的代码，单独为boying机器人创建相关的任务配置文件，但是核心的是，和go1任务相比，除了机器人本体和相关配置参数有变化，其他完全复制go1的任务配置文件，相当于是替换了，但是代码文件是boying单独的，不和go1的代码配置文件有冲突。现在理清我的需求，开始你的修改，你可以启动subagent来分别分析相关的任务所需要的信息。在此之前，你先给自己生成一份详细的提示词和执行计划，有需要决策的点告诉我 

python3 legged_gym/legged_gym/scripts/train.py --task=boying --headless

ok，现在忘记之前的修改，对比boying和go1两个任务，现在还有什么区别，从机器人模型，机器人参数配置，训练的网络结构，训练的超参数，奖励函数，域随机化，课程设计，终止条件，一项项的进行分析。
你查看一下boying机器人，碰撞体是不是有什么特殊的地方，并且终止条件是不是也可以修改一下？只分析
ok，现在可能有两个需要优化的点，就是机器人初始化的高度和目标高度，看看有没有什么区别？以及因为两个机器人本身的重量，模型特点以及机器人的控制参数不一样，需要定制化的奖励函数，你是否有奖励塑型的经验？有的话开始你的分析，给我修改计划
